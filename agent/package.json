{
  "name": "apimatic-gh-app-validator-agent",
  "version": "1.0.0",
  "description": "Automation agent that tests the APIMatic GitHub App by orchestrating PRs in test repos",
  "private": true,
  "type": "module",
  "scripts": {
    "run": "npx tsx index.ts"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "tsx": "^4.19.4",
    "typescript": "^5.8.3",
    "@types/node": "^22.0.0"
  }
}

openapi: '3.0.0'
info:
  title: Sample API
  description: A sample OpenAPI specification for testing APIMatic GitHub App path classification.
  version: '1.0.0'
servers:
  - url: https://api.example.com/v1
paths:
  /health:
    get:
      operationId: getHealth
      summary: Health check
      responses:
        '200':
          description: API is healthy
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: ok
  /version:
    get:
      operationId: getVersion
      summary: Get API version
      responses:
        '200':
          description: Version info
          content:
            application/json:
              schema:
                type: object
                properties:
                  version:
                    type: string
                    example: '1.0.0'
# agent-trigger: 2026-04-21T05:13:43.111Z

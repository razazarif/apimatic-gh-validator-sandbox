openapi: "3.0.0"
info:
  title: "Specs API"
  version: "1.0.0"
paths:
  /status:
    get:
      summary: "Health check"
      responses:
        "200":
          description: "OK"
  /version:
    get:
      summary: "Get API version"
      responses:
        "200":
          description: "Returns version info"
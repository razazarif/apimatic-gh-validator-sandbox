openapi: '3.0.0'
info:
  title: Skip Me API
  description: This spec should be excluded from validation via SkipOpenAPIDefinitionPaths.
  version: '1.0.0'
servers:
  - url: https://api.example.com/v1
paths:
  /skip:
    get:
      operationId: getSkip
      summary: This endpoint should never be validated
      responses:
        '200':
          description: OK
